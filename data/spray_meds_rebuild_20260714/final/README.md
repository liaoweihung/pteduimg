# Spray medicine website export

`spray_meds_final.json` is generated only by `../build_final_json.py` from the
formal CSV tables. It contains 141 formal local products and excludes the five
unresolved records plus all excluded pulmonary and skin/other products.
